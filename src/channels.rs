use eyre::Result;
use crate::config::Config;


/// Channel Manager
#[derive(Debug, Default, Clone)]
pub struct ChannelManager {}

impl ChannelManager {
    /// Constructs a new Channel Manager instance
    pub fn new() -> Self {
        Self::default()
    }
}

impl ChannelManager {

    // TODO:

}